mixin test-description(metadata)
    .container-fluid.mt-4
    .card.mb-4
        .card-header.pb-0.ptit-color.c-white
            h5.mb-10 Tạo mới bài thi
        .card-body
            .container
                .option-container#option-container
                    .test-description.row
                        .col-md-6
                            form#description-form
                                .form-group
                                    label(for='examName') Mã bài thi:
                                    if metadata
                                        input.form-control(type='text' id="examID" value=metadata.TenBaiThi required)
                                    else
                                        input.form-control(type='text' id="examID" value="" readonly)
                                .form-group
                                    label(for='examName') Tên bài thi:
                                    if metadata
                                        input.form-control(type='text' id="examName" value=metadata.TenBaiThi required)
                                    else
                                        input.form-control(type='text' id="examName" required)
                                .form-group
                                    label(for='specificExamTime') Thời gian (phút):
                                    if metadata
                                        input.form-control(type='number' id='examTime' value = metadata.ThoiGianThi required)
                                    else
                                        input.form-control(type='number' id='examTime' required)
                               
                                //- .form-group
                                //-     label(for='numQuestions') Số lượng câu hỏi:
                                //-     if metadata
                                //-         input.form-control(type='number'  id='numQuestions' value = metadata.SoLuongCau )
                                //-     else
                                //-         input.form-control(type='number'  id='numQuestions' value = 0  readonly )
                        .col-md-6
                            form#option-form
                                .form-group
                                    label(for='examName') Luyện tập/ Thực hành(th):
                                    if metadata
                                        input.form-control(type='text' id="examStatus" value=metadata.TrangThai required)
                                    else
                                        input.form-control(type='text' id="examStatus" required)
                                .form-group
                                    label(for='examDescription') Loại bài:
                                    select.form-control#examDescription(onchange='toggleFields()')
                                        option(value='', disabled=true, selected=true) Chưa chọn
                                        option(value='trắc nghiệm') trắc nghiệm
                                        option(value='sql') sql
                                        option(value='tự luận') tự luận
                                .form-group#schemaRunField(style='display:none;')
                                    label(for='schemaRun') Schema run:
                                    if metadata
                                        input.form-control(type='text' id="schemaRun" value=metadata.TrangThai required)
                                    else
                                        input.form-control(type='text' id="schemaRun" required)

                                .form-group#schemaTestcaseField(style='display:none;')
                                    label(for='schemaTestcase') Schema testcase:
                                    if metadata
                                        input.form-control(type='text' id="schemaTestcase" value=metadata.TrangThai required)
                                    else
                                        input.form-control(type='text' id="schemaTestcase" required)
                                              
            .form-group.d-flex.align-items-center
                div.flex-grow-1
                button.btn.ptit-color.text-white.btn-responsive(type='submit', onclick='Save2()' id="createExamBtn") Tạo bài thi

                //- if metadata
                //-     button.btn.ptit-color.text-white.px-4.btn-responsive(onclick=`Save('${metadata.MaBaiThi}')`)
                //-         i.ti-save
                //- else 
                //-     button.btn.ptit-color.text-white.px-4.btn-responsive(onclick='Save()') Lưu danh sách câu hỏi
                        

                    
